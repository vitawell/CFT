# parameters
nc: 3  # number of classes #不能注释，但nc数取决于data中yaml
depth_multiple: 1.00  # model depth multiple
width_multiple: 1.00  # layer channel multiple

# anchors
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 backbone
backbone:
  # [from, number, module, args]
  # Two Stream
  [
   [-1, 1, Focus, [64, 3]],  # 0-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 1-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 3-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 5-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 7-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 9

    #########
   [-4, 1, Focus, [64, 3]],  # 10-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 11-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 13-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 15-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 17-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 19
   
   ######### Add Block #############
   [[4,14], 1, Add, [1]],    # 20 two stream fuse
   [[6,16], 1, Add, [1]],    # 21 two stream fuse
   [[9,19], 1, Add, [1]],    # 22 two stream fuse
  ]

# YOLOv5 head                                     [模块输入channel, 模块输出channel, 模块其他参数]
head:
  [[-1, 1, Conv, [512, 1, 1]],          # 23
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 24
   [[-1,21], 1, Concat, [1]],           # 25 cat backbone P4
   [-1, 3, C3, [512, False]],           # 26

   [-1, 1, Conv, [256, 1, 1]],          # 27
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 28
   [[-1,20], 1, Concat, [1]],           # 29 cat backbone P3
   [-1, 3, C3, [256, False]],           # 30 (P3/8-small)[256, 128, 1, False]

   [-1, 1, Conv, [256, 3, 2]],          # 31
   [[-1,27], 1, Concat, [1]],           # 32 cat head P4
   [-1, 3, C3, [512, False]],           # 33 (P4/16-medium)[256, 256, 1, False]

   [-1, 1, Conv, [512, 3, 2]],          # 34
   [[-1,23], 1, Concat, [1]],           # 35 cat head P5
   [-1, 3, C3, [1024, False]],          # 36 (P5/32-large)[512, 512, 1, False]
   
   ####
   [9, 1, Conv, [512, 1, 1]],           # 37
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,21], 1, Concat, [1]],           # 39 cat backbone P4
   [-1, 3, C3, [512, False]],           # 40

   [-1, 1, Conv, [256, 1, 1]],          # 41
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,20], 1, Concat, [1]],           # 43 cat backbone P3
   [-1, 3, C3, [256, False]],           # 44 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 45
   [[-1,41], 1, Concat, [1]],           # 46 cat head P4
   [-1, 3, C3, [512, False]],           # 47 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 48
   [[-1,37], 1, Concat, [1]],           # 49 cat head P5
   [-1, 3, C3, [1024, False]],          # 50 (P5/32-large)
   
    ####
   [19, 1, Conv, [512, 1, 1]],          # 51
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,21], 1, Concat, [1]],           # 53 cat backbone P4
   [-1, 3, C3, [512, False]],           # 54

   [-1, 1, Conv, [256, 1, 1]],          # 55
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,20], 1, Concat, [1]],           # 57 cat backbone P3
   [-1, 3, C3, [256, False]],           # 58 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 59
   [[-1,55], 1, Concat, [1]],           # 60 cat head P4
   [-1, 3, C3, [512, False]],           # 61 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 62
   [[-1,51], 1, Concat, [1]],           # 63 cat head P5
   [-1, 3, C3, [1024, False]],          # 64 (P5/32-large)
   
   ####
   [[30,44], 1, Concat, [1]],           # 65 (128+128)
   [-1, 1, Conv, [256, 1, 1]],          # 66 [256, 128, 1, 1]
   [[66,58], 1, Concat, [1]],           # 67 (128+128)
   [-1, 1, Conv, [256, 1, 1]],          # 68 [256, 128, 1, 1]
   
   [[33,47], 1, Concat, [1]],           # 69 (256+256)
   [-1, 1, Conv, [512, 1, 1]],          # 70 [512, 256, 1, 1]
   [[70,61], 1, Concat, [1]],           # 71 (256+256)
   [-1, 1, Conv, [512, 1, 1]],          # 72 [512, 256, 1, 1]
   
   [[36,50], 1, Concat, [1]],           # 73 (512+512)
   [-1, 1, Conv, [1024, 1, 1]],         # 74 [1024, 512, 1, 1]
   [[74,64], 1, Concat, [1]],           # 75 (512+512)
   [-1, 1, Conv, [1024, 1, 1]],         # 76 [1024, 512, 1, 1]
   
   [[68, 72, 76], 1, Detect, [nc, anchors]],    # Detect(P3, P4, P5)
  ]
