# parameters
nc: 3  # number of classes #不能注释，但nc数取决于data中yaml
depth_multiple: 1.00  # model depth multiple
width_multiple: 1.00  # layer channel multiple

# anchors
anchors:
  - [10,13, 16,30, 33,23]  # P3/8
  - [30,61, 62,45, 59,119]  # P4/16
  - [116,90, 156,198, 373,326]  # P5/32

# YOLOv5 backbone
backbone:
  # [from, number, module, args]
  # 2+2 Stream
  [
   [-1, 1, Focus, [64, 3]],  # 0-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 1-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 3-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 5-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 7-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 9

    #########
   [-4, 1, Focus, [64, 3]],  # 10-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 11-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 13-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 15-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 17-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 19
   
   ######### Add Block #############
   [[4,14], 1, Add, [1]],    # 20 two stream fuse
   [[6,16], 1, Add, [1]],    # 21 two stream fuse
   [[9,19], 1, Add, [1]],    # 22 two stream fuse
   
    #########
   [-1, 1, Focus, [64, 3]],  # 23-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 24-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 26-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 28-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 30-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 32

    #########
   [-8, 1, Focus, [64, 3]],  # 33-P1/2
   [-1, 1, Conv, [128, 3, 2]],  # 34-P2/4
   [-1, 3, C3, [128]],
   [-1, 1, Conv, [256, 3, 2]],  # 36-P3/8
   [-1, 9, C3, [256]],
   [-1, 1, Conv, [512, 3, 2]],  # 38-P4/16
   [-1, 9, C3, [512]],
   [-1, 1, Conv, [1024, 3, 2]],  # 40-P5/32
   [-1, 1, SPP, [1024, [5, 9, 13]]],
   [-1, 3, C3, [1024, False]],  # 42
   
   ######### Add Block #############
   [[4,14], 1, Add, [1]],    # 43 two stream fuse
   [[6,16], 1, Add, [1]],    # 44 two stream fuse
   [[9,19], 1, Add, [1]],    # 45 two stream fuse
  ]

# YOLOv5 head
head:
  [[22, 1, Conv, [512, 1, 1]],          # 46
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 47
   [[-1,21], 1, Concat, [1]],           # 48 cat backbone P4
   [-1, 3, C3, [512, False]],           # 49

   [-1, 1, Conv, [256, 1, 1]],          # 50
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 51
   [[-1,20], 1, Concat, [1]],           # 52 cat backbone P3
   [-1, 3, C3, [256, False]],           # 53 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 54
   [[-1,27], 1, Concat, [1]],           # 55 cat head P4
   [-1, 3, C3, [512, False]],           # 56 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 57
   [[-1,23], 1, Concat, [1]],           # 58 cat head P5
   [-1, 3, C3, [1024, False]],          # 59 (P5/32-large)
   
    ####
   [19, 1, Conv, [512, 1, 1]],          # 60
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,21], 1, Concat, [1]],           # 62 cat backbone P4
   [-1, 3, C3, [512, False]],           # 63

   [-1, 1, Conv, [256, 1, 1]],          # 64
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,20], 1, Concat, [1]],           # 66 cat backbone P3
   [-1, 3, C3, [256, False]],           # 67 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 68
   [[-1,41], 1, Concat, [1]],           # 69 cat head P4
   [-1, 3, C3, [512, False]],           # 70 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 71
   [[-1,37], 1, Concat, [1]],           # 72 cat head P5
   [-1, 3, C3, [1024, False]],          # 73 (P5/32-large)
   
    ####
   [-1, 1, Conv, [512, 1, 1]],          # 74
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 75
   [[-1,21], 1, Concat, [1]],           # 76 cat backbone P4
   [-1, 3, C3, [512, False]],           # 77

   [-1, 1, Conv, [256, 1, 1]],          # 78
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 79
   [[-1,20], 1, Concat, [1]],           # 80 cat backbone P3
   [-1, 3, C3, [256, False]],           # 81 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 82
   [[-1,27], 1, Concat, [1]],           # 83 cat head P4
   [-1, 3, C3, [512, False]],           # 84 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 85
   [[-1,23], 1, Concat, [1]],           # 86 cat head P5
   [-1, 3, C3, [1024, False]],          # 87 (P5/32-large)
   
    ####
   [19, 1, Conv, [512, 1, 1]],          # 88
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,21], 1, Concat, [1]],           # 90 cat backbone P4
   [-1, 3, C3, [512, False]],           # 91

   [-1, 1, Conv, [256, 1, 1]],          # 92
   [-1, 1, nn.Upsample, [None, 2, 'nearest']],  # 
   [[-1,20], 1, Concat, [1]],           # 94 cat backbone P3
   [-1, 3, C3, [256, False]],           # 95 (P3/8-small)

   [-1, 1, Conv, [256, 3, 2]],          # 96
   [[-1,41], 1, Concat, [1]],           # 97 cat head P4
   [-1, 3, C3, [512, False]],           # 98 (P4/16-medium)

   [-1, 1, Conv, [512, 3, 2]],          # 99
   [[-1,37], 1, Concat, [1]],           # 100 cat head P5
   [-1, 3, C3, [1024, False]],          # 101 (P5/32-large)
   
   ####
   [[44, 47, 50], 1, Detect, [nc, anchors]],    # Detect(P3, P4, P5) 102 depth
   [[30, 33, 36], 1, Detect, [nc, anchors]],    # Detect(P3, P4, P5) 103 add
   
   [[44, 47, 50], 1, Detect, [nc, anchors]],    # Detect(P3, P4, P5) 104 depth
   [[30, 33, 36], 1, Detect, [nc, anchors]],    # Detect(P3, P4, P5) 105 add
   
   
  ]
